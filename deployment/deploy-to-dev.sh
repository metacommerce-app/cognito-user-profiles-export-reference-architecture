export AWS_PROFILE=development
export AWS_SDK_LOAD_CONFIG=1
export AWS_REGION=us-east-2
export REGION=us-east-2 # the AWS region to launch the solution (e.g. us-east-1)
export SECONDARY_REGION=us-west-2 # the AWS region that will serve as backup (e.g. eu-central-1)
export DIST_OUTPUT_BUCKET=mc-dev-cognito-user-profiles-export # bucket where customized code will reside
export SOLUTION_NAME=cognito-user-profiles-export
export VERSION=1.0.1 # version number for the customized code
